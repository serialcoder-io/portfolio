import NavButton from "@/ui/nav-button";

export default function Navbar(){
    return (
        <header>
            <nav>
                <NavButton text="Home" />
            </nav>
        </header>
    );
}